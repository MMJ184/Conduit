[package]
name = "conduit-core"
version = "0.1.0"
edition = "2021"

[features]
test-utils = []

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "1"
which = "6"
dirs = "5"
rayon = "1"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
tempfile = "3"
